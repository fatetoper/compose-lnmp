

let corsCookie = {
  getCookie:(name,options)=>{
    
  },
  //Get cookie name with options:
  // --signed : the cookie requested should be signed
  setCookie:(name, value, options)=>{},

}
module.exports = corsCookie;